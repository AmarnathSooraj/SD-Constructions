import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // ✅ Just omit the `turbo` option completely — Turbopack is only enabled via CLI (e.g., `next dev --turbo`)
};

export default nextConfig;
